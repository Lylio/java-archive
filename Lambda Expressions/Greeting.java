public interface Greeting {

    /*The contents of this interface has to have the
    same signature as the Lambda expression which will
    be using this interface as its type.
     */
    public void perform();
}
